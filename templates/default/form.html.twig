{% extends 'layout.html.twig' %}

{% block page_title %} Form demo {% endblock %}
{% block page_pretitle %} Fullscreen width form {% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-md-12">
            {% embed '@Tabler/embeds/card.html.twig' %}
                {% form_theme form '@Tabler/layout/form_theme.html.twig' %}
                {% block box_before %}{{ form_start(form) }}{% endblock %}
                {% block box_title %}{{ 'A simple form demo'|trans }}{% endblock %}
                {% block box_body %}
                    <div>
                        Using the form theme:
                        <pre>{% verbatim %}{% form_theme form '@Tabler/layout/form_theme.html.twig' %}{% endverbatim %}</pre>
                    </div>
                    <hr>
                    {{ form_widget(form) }}
                {% endblock %}
                {% block box_footer %}
                    <div class="pull-right">
                        <button type="reset" class="btn btn-warning"><i class="icon fas fa-times"></i> {{ 'Cancel'|trans }}</button>
                        <button type="submit" class="btn btn-primary"><i class="icon fas fa-check-square"></i> {{ 'Submit'|trans }}</button>
                    </div>
                {% endblock %}

                {% block box_after %}{{ form_end(form)}}{% endblock %}
            {% endembed %}
        </div>
    </div>
{% endblock %}
